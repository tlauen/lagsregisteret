# Nettvising (GitHub Pages)

Røten (`index.html` + `data/lag.csv`) ligg her. Utan `lag.csv` med innhald viser sida eit hint om å hente data og køyre `skript/publiser_data_til_nettside.py` frå hovudmappa.

Domenet blir: `https://<brukar>.github.io/<depotnamn>/` når Pages er slått på for `/docs` (sjå hovud-README).
